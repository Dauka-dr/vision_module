# Зависимости проекта. Версии закреплены жёстко — стек OpenMMLab не обновлялся
# с 2023-24, и произвольные комбинации не собираются. Порядок установки важен:
# pip install -r requirements.txt одной командой НЕ сработает, потому что torch и
# mmcv ставятся с отдельных индексов. Используйте install.ps1 или шаги ниже.
#
# --- шаг 1: PyTorch (отдельный индекс) --------------------------------------
# pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.1.0
torchvision==0.16.0

# --- шаг 2: numpy ДО остальных ----------------------------------------------
# Стек собран под numpy 1.x; с 2.x падает с "compiled using NumPy 1.x cannot be
# run in NumPy 2.x". Ставить до mmcv, иначе он подтянет numpy 2 как зависимость.
numpy==1.26.4

# --- шаг 3: OpenMMLab (mmcv с отдельного индекса) ---------------------------
# pip install -U openmim
# mim install mmengine==0.10.7
# mim install "mmcv==2.1.0"
# ВАЖНО: не mmcv 2.2.0 — mmdet 3.3.0 жёстко ассертит mmcv<2.2.0 при импорте.
openmim==0.3.9
mmengine==0.10.7
mmcv==2.1.0
mmdet==3.3.0
mmpose==1.3.2

# --- шаг 4: mmaction2 из исходников ------------------------------------------
# git clone https://github.com/open-mmlab/mmaction2.git
# pip install -e mmaction2
# (нужен как исходники ради конфигов ST-GCN++ / PoseC3D)

# --- шаг 5: остальное --------------------------------------------------------
# opencv 5.x требует numpy>=2 — конфликт со стеком, поэтому 4.x
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80
scipy==1.15.3
matplotlib==3.10.9

# Транзитивная зависимость mmpose. Оригинальный chumpy на PyPI не собирается на
# современном setuptools ("No module named 'pip'"), нужен git-форк:
# pip install --no-build-isolation "chumpy @ git+https://github.com/mattloper/chumpy"
